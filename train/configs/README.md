# Training configs

This directory stores reusable training presets.

## Contents

- `baseline.json` — default PPO baseline configuration

## Conventions

- keep config files small and human-readable
- prefer explicit values over hidden defaults
- keep environment backend choices explicit (`auto`, `mock`, `real`)
