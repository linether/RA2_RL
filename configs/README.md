# Configs

This directory stores project configuration files for training, evaluation, and runtime setup.

Structure:

- `training/` — PPO and training presets
- `evaluation/` — eval and benchmark settings
- `runtime/` — environment and runtime-specific configurations
